# ===============================
# SERVER
# ===============================
server.port=8080

# ===============================
# DATASOURCE (PostgreSQL)
# ===============================
spring.datasource.url=jdbc:postgresql://dpg-d6ea57fgi27c73csro10-a:5432/smarttechsolution_database
spring.datasource.username=smarttechsolution_database_user
spring.datasource.password=Z1Jfp8k42QqIyPCFbHI4jc7PFD9DKrj0
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable Open Session in View (recommended)
spring.jpa.open-in-view=false

# ===============================
# SECURITY (TEMP ? JWT later)
# ===============================
spring.security.user.name=admin
spring.security.user.password=admin
